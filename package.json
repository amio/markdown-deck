{
  "name": "markdown-deck",
  "version": "1.3.1",
  "description": "A web component for presenters",
  "repository": "amio/markdown-deck",
  "main": "dist/markdown-deck.min.js",
  "license": "MIT",
  "scripts": {
    "start": "bili -w --format iife & es-dev-server -a index.html -o",
    "build": "rm -rf dist && bili --format iife --minify",
    "build-deck": "rm -rf public && mkdir public && cp -R dist public && cp *.html *.md public",
    "now-build": "npm run build && npm run build-deck",
    "prepack": "npm run build"
  },
  "dependencies": {
    "lit-element": "2.2.1",
    "lit-html": "^1.1.2",
    "marked": "^0.7.0",
    "prismjs": "^1.17.1"
  },
  "devDependencies": {
    "bili": "^4.8.1",
    "es-dev-server": "^1.18.3",
    "rollup-plugin-typescript2": "^0.24.3",
    "typescript": "^3.6.4"
  }
}
